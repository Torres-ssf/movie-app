import styled from 'styled-components';

export const Container = styled.article`
  border-radius: 5px;
  margin: 10px;

  figure {
    img {
      border-radius: 5px;
      object-fit: cover;
      max-width: 100%;
    }
  }
`;
